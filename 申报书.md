# 2026 MoonBit 国产基础软件开源大赛 · 项目申报书

## 一、 项目基本信息
- **项目名称**：MoonTraceKit：轻量级一体化可观测性与全链路追踪 SDK
- **项目标识**：`moon_tracekit`
- **参赛赛道**：交叉赛道（分布式架构工具链 × 日志 + Tracing + Metrics + Async）
- **参赛仓库链接**：
  - **GitLink 官方主提交仓库**：https://gitlink.org.cn/zhzh12345678/moon_tracekit
  - **GitHub 镜像同步仓库**：https://github.com/zhzh12345678/moon_tracekit
- **作品来源说明**：**100% 原创作品**。自底层零依赖构建，非第三方库移植或二次封装，无商业版权风险。

## 二、 项目简介与痛点解决（272字）
MoonTraceKit 是专为 MoonBit 生态打造的轻量级、一体化全链路可观测性 SDK。项目创新性提出统一遥测模型（TelemetryEvent），在单一框架内无缝融合分布式追踪（Tracing）、结构化日志（Logging）与实时度量（Metrics），打破监控数据孤岛。SDK 内置微秒级高精度计时、跨函数异步上下文安全透传以及 P50/P90/P99 耗时统计。针对“大赛验收”核心痛点，独创终端交互式 ASCII 瀑布流时序图渲染引擎与 OTel JSONL 兼容流，评委一键即可直观洞察微服务链路瓶颈，具备极强的工程复用与扩展价值。

## 三、 核心技术架构与特色功能
1. **统一遥测模型（Unified Telemetry Model）**：独创 `TelemetryEvent` 三态模型，日志与指标自动关联当前活跃 Span 的 `trace_id` / `span_id`，实现全链路因果关系绑定。
2. **零侵入异步上下文透传（Async Propagation）**：基于函数式闭包包装器 `with_span(...)`，优雅解决微服务跨层级调用与异步回调中的上下文丢失难题。
3. **大赛极速验收大盘（ASCII Timeline Waterfall）**：零外部依赖，命令行一键渲染微秒级高并发调用瀑布流图与分位数耗时分布，让评审与调试直观震撼。
4. **多格式后端适配（Multi-Destination Exporters）**：支持标准 OpenTelemetry (OTel) 兼容的 JSON Lines 流式持久化与测试内存缓冲。

## 四、 实施路径与交付成果
- **已交付基础模块**：完全解耦的 7 大功能子包（`core`, `span`, `log`, `metric`, `exporter`, `tracer`, `demo`）及 1 个 CLI 工具（`cmd/trace_demo`）。
- **质量与工程作证**：严格遵循 MoonBit 1.0+ 现代语法，本地编译器零错误、零警告；单元测试覆盖率 100%；配置完整中英双语 README、Apache-2.0 许可证与 GitHub/GitLink 持续集成（CI）流水线。
